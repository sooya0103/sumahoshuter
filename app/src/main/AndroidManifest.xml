<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smahoshooter">

    <uses-permission android:name="android.permission.CAMERA" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Smahoshooter">
        <activity android:name=".kekka2"></activity>
        <activity android:name=".room2" />
        <activity android:name=".room" />
        <activity android:name=".keikoku" />
        <activity android:name=".name" />
        <activity android:name=".passname" />
        <activity android:name=".pass" />
        <activity android:name=".battleroom" />
        <activity android:name=".camera3" />
        <activity android:name=".camera" />
        <activity android:name=".kekka" />
        <activity android:name=".start">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>